# Optional configuration reference. The application does not read this file automatically.
# No API keys are required for the core web app or offline project forecasts.
# To use a custom model environment in PowerShell, set:
# $env:WIND_AGENT_MODEL_PYTHON = "D:\environments\wind-model\Scripts\python.exe"
# Local .venv-model is detected automatically; see README.md for installation.
